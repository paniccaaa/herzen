# Лекция 2: Визуализация данных для образовательной аналитики

**Продолжительность:** 2 академических часа

## План лекции

1. Введение в визуализацию данных (10 минут)
2. Библиотека matplotlib (30 минут)
3. Библиотека seaborn (30 минут)
4. Создание дашбордов (25 минут)
5. Best practices (15 минут)

---

## 1. Введение в визуализацию данных

### Почему визуализация важна?

**Цитата:** "Одна картинка стоит тысячи слов"

**Преимущества визуализации:**
- 📊 Быстрое понимание данных
- 📈 Видны тренды и закономерности
- 🎯 Легче донести информацию до родителей и администрации
- 💡 Помогает принимать решения
- 🔍 Выявляет аномалии

### Типы графиков и когда их использовать

| Тип графика | Когда использовать | Пример в образовании |
|-------------|-------------------|---------------------|
| Столбчатая диаграмма | Сравнение категорий | Средние баллы по предметам |
| Линейный график | Показать изменения во времени | Динамика успеваемости по четвертям |
| Круговая диаграмма | Показать доли целого | Распределение оценок в классе |
| Гистограмма | Распределение данных | Распределение баллов за контрольную |
| Box plot | Показать разброс данных | Сравнение разброса оценок между классами |
| Scatter plot | Показать корреляцию | Связь между посещаемостью и оценками |
| Heatmap | Показать матрицу значений | Оценки учеников по всем предметам |

---

## 2. Библиотека matplotlib

### Установка и импорт

```bash
pip install matplotlib
```

```python
import matplotlib.pyplot as plt
import pandas as pd
import numpy as np
```

### Базовый пример

```python
# Простой линейный график
x = [1, 2, 3, 4, 5]
y = [2, 4, 6, 8, 10]

plt.plot(x, y)
plt.xlabel('X ось')
plt.ylabel('Y ось')
plt.title('Простой график')
plt.show()
```

### Столбчатая диаграмма - Средние баллы по предметам

```python
# Данные
subjects = ['Математика', 'Русский', 'Физика', 'Информатика', 'История']
averages = [4.2, 3.8, 4.0, 4.5, 3.9]

# Создание графика
plt.figure(figsize=(10, 6))
plt.bar(subjects, averages, color='skyblue', edgecolor='navy')
plt.xlabel('Предмет', fontsize=12)
plt.ylabel('Средний балл', fontsize=12)
plt.title('Средние баллы по предметам в 9А классе', fontsize=14, fontweight='bold')
plt.ylim(0, 5)  # Ось Y от 0 до 5

# Добавляем значения над столбцами
for i, v in enumerate(averages):
    plt.text(i, v + 0.1, str(v), ha='center', va='bottom', fontweight='bold')

plt.tight_layout()
plt.show()
```

### Линейный график - Динамика успеваемости

```python
# Данные по четвертям
quarters = ['1 четверть', '2 четверть', '3 четверть', '4 четверть']
ivanov = [4.2, 4.3, 4.5, 4.6]
petrova = [3.8, 3.9, 4.1, 4.2]
sidorov = [3.5, 3.4, 3.6, 3.8]

# Создание графика
plt.figure(figsize=(12, 6))
plt.plot(quarters, ivanov, marker='o', linewidth=2, label='Иванов')
plt.plot(quarters, petrova, marker='s', linewidth=2, label='Петрова')
plt.plot(quarters, sidorov, marker='^', linewidth=2, label='Сидоров')

plt.xlabel('Четверть', fontsize=12)
plt.ylabel('Средний балл', fontsize=12)
plt.title('Динамика успеваемости учеников', fontsize=14, fontweight='bold')
plt.legend(loc='lower right', fontsize=10)
plt.grid(True, alpha=0.3)
plt.ylim(3, 5)

plt.tight_layout()
plt.show()
```

### Круговая диаграмма - Распределение оценок

```python
# Данные
grades = ['5', '4', '3', '2']
counts = [8, 12, 5, 1]
colors = ['#4CAF50', '#8BC34A', '#FFC107', '#F44336']

# Создание графика
plt.figure(figsize=(8, 8))
plt.pie(counts, labels=grades, colors=colors, autopct='%1.1f%%', 
        startangle=90, textprops={'fontsize': 14, 'fontweight': 'bold'})
plt.title('Распределение оценок по математике\n9А класс', fontsize=14, fontweight='bold')
plt.axis('equal')

plt.tight_layout()
plt.show()
```

### Гистограмма - Распределение баллов

```python
# Баллы за контрольную работу
scores = [45, 67, 89, 78, 92, 56, 73, 85, 90, 88, 76, 
          82, 79, 91, 68, 74, 87, 83, 77, 94, 71, 86]

# Создание гистограммы
plt.figure(figsize=(10, 6))
plt.hist(scores, bins=10, color='lightblue', edgecolor='black', alpha=0.7)
plt.xlabel('Баллы', fontsize=12)
plt.ylabel('Количество учеников', fontsize=12)
plt.title('Распределение баллов за контрольную работу', fontsize=14, fontweight='bold')
plt.axvline(np.mean(scores), color='red', linestyle='--', linewidth=2, label=f'Среднее: {np.mean(scores):.1f}')
plt.legend()
plt.grid(axis='y', alpha=0.3)

plt.tight_layout()
plt.show()
```

### Несколько графиков на одной фигуре

```python
# Создание фигуры с подграфиками
fig, axes = plt.subplots(2, 2, figsize=(14, 10))

# График 1: Столбчатая диаграмма
subjects = ['Матем.', 'Русский', 'Физика', 'Инф.']
averages = [4.2, 3.8, 4.0, 4.5]
axes[0, 0].bar(subjects, averages, color='skyblue')
axes[0, 0].set_title('Средние баллы по предметам')
axes[0, 0].set_ylim(0, 5)

# График 2: Линейный график
quarters = ['1 кв.', '2 кв.', '3 кв.', '4 кв.']
performance = [4.0, 4.1, 4.3, 4.4]
axes[0, 1].plot(quarters, performance, marker='o', linewidth=2, color='green')
axes[0, 1].set_title('Динамика среднего балла класса')
axes[0, 1].grid(True, alpha=0.3)

# График 3: Круговая диаграмма
grades = ['5', '4', '3']
counts = [10, 15, 5]
axes[1, 0].pie(counts, labels=grades, autopct='%1.1f%%', startangle=90)
axes[1, 0].set_title('Распределение оценок')

# График 4: Гистограмма
scores = np.random.normal(75, 15, 30)
axes[1, 1].hist(scores, bins=10, color='lightcoral', edgecolor='black')
axes[1, 1].set_title('Распределение баллов')
axes[1, 1].set_xlabel('Баллы')

plt.tight_layout()
plt.show()
```

---

## 3. Библиотека seaborn

### Установка и импорт

```bash
pip install seaborn
```

```python
import seaborn as sns
import matplotlib.pyplot as plt
import pandas as pd

# Установка стиля
sns.set_style("whitegrid")
```

### Box plot - Сравнение классов

```python
# Создание данных
data = {
    'Класс': ['9А']*10 + ['9Б']*10 + ['9В']*10,
    'Балл': [85, 90, 78, 92, 88, 76, 95, 82, 87, 91,  # 9А
             72, 75, 80, 78, 85, 73, 88, 76, 82, 79,  # 9Б
             88, 92, 86, 90, 94, 87, 91, 89, 93, 85]  # 9В
}
df = pd.DataFrame(data)

# Box plot
plt.figure(figsize=(10, 6))
sns.boxplot(x='Класс', y='Балл', data=df, palette='Set2')
plt.title('Сравнение результатов контрольной работы по классам', fontsize=14, fontweight='bold')
plt.ylabel('Баллы', fontsize=12)
plt.xlabel('Класс', fontsize=12)

plt.tight_layout()
plt.show()
```

### Heatmap - Оценки учеников по предметам

```python
# Создание данных
students_data = {
    'Ученик': ['Иванов', 'Петрова', 'Сидоров', 'Козлова', 'Смирнов'],
    'Математика': [5, 4, 3, 5, 4],
    'Русский': [4, 5, 4, 4, 3],
    'Физика': [5, 4, 3, 5, 4],
    'Информатика': [5, 5, 4, 5, 5],
    'История': [4, 4, 3, 4, 3]
}
df = pd.DataFrame(students_data)
df = df.set_index('Ученик')

# Тепловая карта
plt.figure(figsize=(10, 6))
sns.heatmap(df, annot=True, fmt='d', cmap='RdYlGn', cbar_kws={'label': 'Оценка'},
            vmin=2, vmax=5, linewidths=0.5, linecolor='gray')
plt.title('Оценки учеников по предметам', fontsize=14, fontweight='bold')
plt.xlabel('Предмет', fontsize=12)
plt.ylabel('Ученик', fontsize=12)

plt.tight_layout()
plt.show()
```

### Scatter plot - Корреляция посещаемости и оценок

```python
# Создание данных
data = {
    'Посещаемость_%': [95, 88, 72, 98, 85, 65, 92, 78, 90, 70, 96, 82, 75, 93, 68],
    'Средний_балл': [4.8, 4.3, 3.5, 4.9, 4.2, 3.2, 4.6, 3.8, 4.5, 3.4, 4.7, 4.0, 3.6, 4.6, 3.3]
}
df = pd.DataFrame(data)

# Scatter plot с линией тренда
plt.figure(figsize=(10, 6))
sns.regplot(x='Посещаемость_%', y='Средний_балл', data=df, 
            scatter_kws={'s': 100, 'alpha': 0.7}, line_kws={'color': 'red'})
plt.title('Зависимость успеваемости от посещаемости', fontsize=14, fontweight='bold')
plt.xlabel('Посещаемость (%)', fontsize=12)
plt.ylabel('Средний балл', fontsize=12)
plt.grid(True, alpha=0.3)

plt.tight_layout()
plt.show()
```

### Violin plot - Распределение с деталями

```python
# Создание данных
data = {
    'Предмет': ['Математика']*20 + ['Физика']*20 + ['Информатика']*20,
    'Балл': np.random.normal(4.0, 0.8, 20).tolist() + 
            np.random.normal(3.8, 0.9, 20).tolist() + 
            np.random.normal(4.3, 0.7, 20).tolist()
}
df = pd.DataFrame(data)
df['Балл'] = df['Балл'].clip(2, 5)  # Ограничиваем от 2 до 5

# Violin plot
plt.figure(figsize=(10, 6))
sns.violinplot(x='Предмет', y='Балл', data=df, palette='muted')
plt.title('Распределение оценок по предметам', fontsize=14, fontweight='bold')
plt.ylabel('Баллы', fontsize=12)

plt.tight_layout()
plt.show()
```

---

## 4. Создание дашбордов

### Комплексный дашборд класса

```python
import matplotlib.pyplot as plt
import seaborn as sns
import pandas as pd
import numpy as np

# Установка стиля
sns.set_style("whitegrid")
plt.rcParams['font.family'] = 'DejaVu Sans'

# Создание данных
np.random.seed(42)
students = [f'Ученик_{i}' for i in range(1, 21)]
math = np.random.randint(3, 6, 20)
russian = np.random.randint(3, 6, 20)
physics = np.random.randint(3, 6, 20)
informatics = np.random.randint(3, 6, 20)

df = pd.DataFrame({
    'Ученик': students,
    'Математика': math,
    'Русский': russian,
    'Физика': physics,
    'Информатика': informatics
})

df['Средний_балл'] = df[['Математика', 'Русский', 'Физика', 'Информатика']].mean(axis=1)

# Создание дашборда
fig = plt.figure(figsize=(16, 12))
fig.suptitle('Дашборд успеваемости 9А класса', fontsize=20, fontweight='bold', y=0.98)

# 1. Средние баллы по предметам
ax1 = plt.subplot(3, 3, 1)
subjects = ['Математика', 'Русский', 'Физика', 'Информатика']
averages = [df[subj].mean() for subj in subjects]
bars = ax1.bar(subjects, averages, color=['#3498db', '#e74c3c', '#2ecc71', '#f39c12'])
ax1.set_title('Средние баллы по предметам', fontweight='bold', fontsize=12)
ax1.set_ylim(0, 5)
ax1.set_ylabel('Балл')
for bar in bars:
    height = bar.get_height()
    ax1.text(bar.get_x() + bar.get_width()/2., height + 0.1,
             f'{height:.2f}', ha='center', va='bottom', fontweight='bold')

# 2. Распределение оценок
ax2 = plt.subplot(3, 3, 2)
all_grades = pd.concat([df[subj] for subj in subjects])
grade_counts = all_grades.value_counts().sort_index()
colors_pie = ['#e74c3c', '#f39c12', '#2ecc71', '#3498db']
ax2.pie(grade_counts.values, labels=grade_counts.index, colors=colors_pie, 
        autopct='%1.1f%%', startangle=90)
ax2.set_title('Распределение оценок', fontweight='bold', fontsize=12)

# 3. Топ-5 учеников
ax3 = plt.subplot(3, 3, 3)
top_students = df.nlargest(5, 'Средний_балл')[['Ученик', 'Средний_балл']]
ax3.barh(top_students['Ученик'], top_students['Средний_балл'], color='#2ecc71')
ax3.set_title('Топ-5 учеников', fontweight='bold', fontsize=12)
ax3.set_xlabel('Средний балл')
ax3.invert_yaxis()

# 4. Тепловая карта оценок
ax4 = plt.subplot(3, 3, (4, 6))
heatmap_data = df.set_index('Ученик')[subjects]
sns.heatmap(heatmap_data, annot=True, fmt='d', cmap='RdYlGn', vmin=2, vmax=5,
            cbar_kws={'label': 'Оценка'}, linewidths=0.5, ax=ax4)
ax4.set_title('Оценки всех учеников по предметам', fontweight='bold', fontsize=12)

# 5. Гистограмма средних баллов
ax5 = plt.subplot(3, 3, 7)
ax5.hist(df['Средний_балл'], bins=10, color='skyblue', edgecolor='black', alpha=0.7)
ax5.axvline(df['Средний_балл'].mean(), color='red', linestyle='--', 
            linewidth=2, label=f'Среднее: {df["Средний_балл"].mean():.2f}')
ax5.set_title('Распределение средних баллов', fontweight='bold', fontsize=12)
ax5.set_xlabel('Средний балл')
ax5.set_ylabel('Количество учеников')
ax5.legend()

# 6. Box plot по предметам
ax6 = plt.subplot(3, 3, 8)
df_melted = df[subjects].melt(var_name='Предмет', value_name='Балл')
sns.boxplot(x='Предмет', y='Балл', data=df_melted, palette='Set2', ax=ax6)
ax6.set_title('Разброс оценок по предметам', fontweight='bold', fontsize=12)
ax6.set_xticklabels(ax6.get_xticklabels(), rotation=45, ha='right')

# 7. Статистика класса
ax7 = plt.subplot(3, 3, 9)
ax7.axis('off')
stats_text = f"""
СТАТИСТИКА КЛАССА

Всего учеников: {len(df)}

Средний балл класса: {df['Средний_балл'].mean():.2f}

Отличников (≥4.5): {len(df[df['Средний_балл'] >= 4.5])}
Хорошистов (≥3.5): {len(df[(df['Средний_балл'] >= 3.5) & (df['Средний_балл'] < 4.5)])}
Троечников: {len(df[df['Средний_балл'] < 3.5])}

Лучший предмет: {subjects[np.argmax(averages)]}
Сложный предмет: {subjects[np.argmin(averages)]}
"""
ax7.text(0.1, 0.9, stats_text, transform=ax7.transAxes, fontsize=11,
         verticalalignment='top', fontfamily='monospace',
         bbox=dict(boxstyle='round', facecolor='wheat', alpha=0.5))

plt.tight_layout()
plt.show()
```

---

## 5. Best Practices

### Правила хорошей визуализации

1. **Выбирайте правильный тип графика**
   - Не используйте круговую диаграмму для более чем 5-6 категорий
   - Используйте линейные графики для временных рядов
   - Box plot лучше для сравнения распределений

2. **Цвета**
   - Используйте контрастные цвета
   - Учитывайте дальтонизм (избегайте красно-зелёной схемы)
   - Не используйте более 5-7 цветов на одном графике

3. **Подписи и заголовки**
   - Всегда добавляйте заголовок
   - Подписывайте оси
   - Добавляйте единицы измерения

4. **Простота**
   - Не перегружайте график
   - Убирайте лишние элементы
   - Один график - одна идея

5. **Доступность**
   - Используйте достаточный размер шрифта
   - Добавляйте легенды
   - Делайте графики читаемыми в чёрно-белом варианте

### Сохранение графиков

```python
# Сохранение в файл
plt.savefig('график.png', dpi=300, bbox_inches='tight')
plt.savefig('график.pdf', bbox_inches='tight')  # Для печати
plt.savefig('график.svg', bbox_inches='tight')  # Векторный формат
```

---

## Домашнее задание

Создайте дашборд для вашего класса, который включает:
1. Средние баллы по предметам (столбчатая диаграмма)
2. Динамику успеваемости топ-3 учеников (линейный график)
3. Распределение оценок (круговая диаграмма)
4. Тепловую карту оценок учеников по предметам
5. Статистический блок с ключевыми показателями

Сохраните дашборд в файл PNG.

---

## Вопросы для самопроверки

1. Какой тип графика использовать для показа изменений во времени?
2. Что показывает box plot?
3. Как добавить заголовок к графику?
4. Как сохранить график в файл?
5. Что такое heatmap и когда его использовать?

---

## Полезные ресурсы

- [Matplotlib Gallery](https://matplotlib.org/stable/gallery/index.html)
- [Seaborn Gallery](https://seaborn.pydata.org/examples/index.html)
- [Data Visualization Catalogue](https://datavizcatalogue.com/)
- [Python Graph Gallery](https://python-graph-gallery.com/)

